 #include <iostream>
 using namespace std;
 int countDigits(int n)
{
   if (n==0)
   return 1;
   if(n/10>0)
   return 1 + countDigits(n/10);
   return countDigits(n/10);
  
}
int main()
{
CountDigits(18792);
}

